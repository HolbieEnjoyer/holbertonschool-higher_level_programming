#/bin/bash
apt-get install dos2unix
git config --global user.name "HolbieEnjoyer"
git config --global user.email "8698@holbertonstudents.com"
chmod -R +x .
find . -type f -exec dos2unix {} \;
git add --all
git commit -m "Next task"
git push -u origin main
